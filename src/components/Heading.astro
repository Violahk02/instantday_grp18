---
// Henter parametrene fra Astro.props
const { htmlheading="h2", size="medium", font="sans", stylingclasses=""} = Astro.props; 
const Htmlheading = htmlheading;
 
//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige størrelser.
const sizeClasses = {
"xl-large": "text-7xl lg:text-9xl text-ashy-black",
"large": "text-5xl lg:text-7xl text-ashy-black",
"medium": "text-2xl lg:text-5xl text-ashy-black",
"small": "text-sm lg:text-lg text-ashy-black",
};

//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige skrifttyper.
const fontClasses = {
  "lilita-one": "font-display",
  "open-sans": "font-sans",
};

---
<Htmlheading class:list={[sizeClasses[size], fontClasses[font], stylingclasses]}><slot /></Htmlheading>